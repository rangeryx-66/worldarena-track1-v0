diff --git a/description/utils/generate_episode_instructions.py b/description/utils/generate_episode_instructions.py
index f3f00c9..40544dc 100644
--- a/description/utils/generate_episode_instructions.py
+++ b/description/utils/generate_episode_instructions.py
@@ -136,9 +136,16 @@ def load_task_instructions(task_name: str) -> Dict[str, Any]:
     return task_data
 
 
+def data_root_path(scene_info_path: str) -> str:
+    """Resolve RoboTwin save_path, preserving absolute paths from task config."""
+    if os.path.isabs(scene_info_path):
+        return scene_info_path
+    return os.path.abspath(os.path.join(parent_directory, "../..", scene_info_path))
+
+
 def load_scene_info(task_name: str, setting: str, scene_info_path: str) -> Dict[str, Dict]:
     """Load the scene info from the JSON file in the data directory."""
-    file_path = os.path.join(parent_directory, f"../../{scene_info_path}/{task_name}/{setting}/scene_info.json")
+    file_path = os.path.join(data_root_path(scene_info_path), task_name, setting, "scene_info.json")
     try:
         with open(file_path, "r") as f:
             scene_data = json.load(f)
@@ -162,9 +169,9 @@ def extract_episodes_from_scene_info(scene_info: Dict) -> List[Dict[str, str]]:
     return episodes
 
 
-def save_episode_descriptions(task_name: str, setting: str, generated_descriptions: List[Dict]):
+def save_episode_descriptions(task_name: str, setting: str, generated_descriptions: List[Dict], save_path: str = "data"):
     """Save generated descriptions to output files."""
-    output_dir = os.path.join(parent_directory, f"../../data/{task_name}/{setting}/instructions")
+    output_dir = os.path.join(data_root_path(save_path), task_name, setting, "instructions")
     os.makedirs(output_dir, exist_ok=True)
 
     for episode_desc in generated_descriptions:
@@ -272,5 +279,5 @@ if __name__ == "__main__":
     results = generate_episode_descriptions(args.task_name, episodes, args.max_num)
 
     # Save results to output files
-    save_episode_descriptions(args.task_name, args.setting, results)
+    save_episode_descriptions(args.task_name, args.setting, results, args_dict['save_path'])
     print("Successfully Saved Instructions")
\ No newline at end of file
diff --git a/envs/_base_task.py b/envs/_base_task.py
index 982e6ec..dd9c17b 100644
--- a/envs/_base_task.py
+++ b/envs/_base_task.py
@@ -64,6 +64,7 @@ class Base_Task(gym.Env):
         self.save_dir = kwags.get("save_path", "data")
         self.ep_num = kwags.get("now_ep_num", 0)
         self.render_freq = kwags.get("render_freq", 10)
+        self.render_settings = kwags.get("render", {}) or {}
         self.data_type = kwags.get("data_type", None)
         self.save_data = kwags.get("save_data", False)
         self.dual_arm = kwags.get("dual_arm", True)
@@ -78,6 +79,14 @@ class Base_Task(gym.Env):
         self.clean_background_rate = random_setting.get("clean_background_rate", 1)
         self.random_head_camera_dis = random_setting.get("random_head_camera_dis", 0)
         self.random_table_height = random_setting.get("random_table_height", 0)
+        # Numeric randomization fields: bool True becomes 1 in Python and creates
+        # absurd camera/table offsets. Treat bools as disabled and clamp distances.
+        if isinstance(self.random_head_camera_dis, bool):
+            self.random_head_camera_dis = 0.0
+        if isinstance(self.random_table_height, bool):
+            self.random_table_height = 0.0
+        self.random_head_camera_dis = float(np.clip(float(self.random_head_camera_dis or 0.0), 0.0, 0.08))
+        self.random_table_height = float(np.clip(float(self.random_table_height or 0.0), 0.0, 0.05))
         self.random_light = random_setting.get("random_light", False)
         self.crazy_random_light_rate = random_setting.get("crazy_random_light_rate", 0)
         self.crazy_random_light = (0 if not self.random_light else np.random.rand() < self.crazy_random_light_rate)
@@ -211,10 +220,20 @@ class Base_Task(gym.Env):
         # give renderer to sapien sim
         self.engine.set_renderer(self.renderer)
 
-        sapien.render.set_camera_shader_dir("rt")
-        sapien.render.set_ray_tracing_samples_per_pixel(32)
-        sapien.render.set_ray_tracing_path_depth(8)
-        sapien.render.set_ray_tracing_denoiser("oidn")
+        render_settings = getattr(self, "render_settings", {}) or {}
+        camera_shader_dir = render_settings.get("camera_shader_dir", "rt")
+        ray_tracing_spp = int(render_settings.get("ray_tracing_samples_per_pixel", 256))
+        ray_tracing_path_depth = int(render_settings.get("ray_tracing_path_depth", 8))
+        ray_tracing_denoiser = render_settings.get("ray_tracing_denoiser", "optix")
+        if isinstance(ray_tracing_denoiser, str) and ray_tracing_denoiser.lower() in ("", "none", "null", "false"):
+            ray_tracing_denoiser = None
+
+        sapien.render.set_camera_shader_dir(camera_shader_dir)
+        if camera_shader_dir == "rt":
+            sapien.render.set_ray_tracing_samples_per_pixel(ray_tracing_spp)
+            sapien.render.set_ray_tracing_path_depth(ray_tracing_path_depth)
+            if ray_tracing_denoiser:
+                sapien.render.set_ray_tracing_denoiser(ray_tracing_denoiser)
 
         # declare sapien scene
         scene_config = sapien.SceneConfig()
diff --git a/envs/camera/camera.py b/envs/camera/camera.py
index 46d960d..92029e2 100644
--- a/envs/camera/camera.py
+++ b/envs/camera/camera.py
@@ -49,7 +49,11 @@ class Camera:
         self.pcd_crop_bbox = kwags.get("bbox", [[-0.6, -0.35, 0.7401], [0.6, 0.35, 2]])
         self.pcd_crop_bbox[0][2] += bias
         self.table_z_bias = bias
-        self.random_head_camera_dis = random_head_camera_dis
+        # This value is a numeric distance. YAML bool True would become 1.0m and
+        # can push the head camera out of the workspace, producing empty videos.
+        if isinstance(random_head_camera_dis, bool):
+            random_head_camera_dis = 0.0
+        self.random_head_camera_dis = float(np.clip(float(random_head_camera_dis or 0.0), 0.0, 0.08))
 
         self.static_camera_config = []
         self.head_camera_type = kwags["camera"].get("head_camera_type", "D435")
